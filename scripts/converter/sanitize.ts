/** Node-side wrapper: shared sanitizer with happy-dom injected. */
import "./dom.js";

export { sanitizeFoundryHtml, sanitizeFoundryEnricherLabels, sanitizeHtml } from "../../src/statblock/sanitize.js";
