/**
 * Module constants.
 * This file has NO imports to avoid circular dependency issues.
 */

export const MODULE_ID = "codex-foundry";
/** Pre-rename module id. Read fallback only — all writes use MODULE_ID. */
export const LEGACY_MODULE_ID = "sf2e-forge-custom";
