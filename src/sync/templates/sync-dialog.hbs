<div class="forge-sync-dialog">
    {{#if report}}
        <section class="forge-sync-report">
            <h3>{{localize "sf2e-forge-custom.sync.reportTitle"}}</h3>
            <ul class="forge-sync-report-counts">
                <li>{{localize "sf2e-forge-custom.sync.reportCreated" count=report.created}}</li>
                <li>{{localize "sf2e-forge-custom.sync.reportUpdated" count=report.updated}}</li>
                <li>{{localize "sf2e-forge-custom.sync.reportAdopted" count=report.adopted}}</li>
                <li>{{localize "sf2e-forge-custom.sync.reportReimported" count=report.reimported}}</li>
                <li>{{localize "sf2e-forge-custom.sync.reportDeleted" count=report.deleted}}</li>
            </ul>
            {{#if report.failed.length}}
                <h4>{{localize "sf2e-forge-custom.sync.reportFailuresTitle"}}</h4>
                <ul class="forge-sync-report-failures">
                    {{#each report.failed}}
                        <li><strong>{{name}}</strong> — {{error}}</li>
                    {{/each}}
                </ul>
            {{/if}}
        </section>
    {{else}}
        {{#if unchanged}}
            <p class="forge-sync-unchanged">{{localize "sf2e-forge-custom.sync.unchanged" count=unchanged}}</p>
        {{/if}}

        {{#if adopt.length}}
            <section class="forge-sync-section">
                <h3>{{localize "sf2e-forge-custom.sync.sectionAdopt"}}</h3>
                <ul>
                    {{#each adopt}}
                        <li>
                            <label>
                                <input type="checkbox" name="adopt" value="{{key}}" checked>
                                {{name}} <span class="forge-sync-meta">({{docType}})</span>
                            </label>
                        </li>
                    {{/each}}
                </ul>
            </section>
        {{/if}}

        {{#if create.length}}
            <section class="forge-sync-section">
                <h3>{{localize "sf2e-forge-custom.sync.sectionCreate"}}</h3>
                <ul>
                    {{#each create}}
                        <li>{{name}} <span class="forge-sync-meta">({{docType}})</span></li>
                    {{/each}}
                </ul>
            </section>
        {{/if}}

        {{#if update.length}}
            <section class="forge-sync-section">
                <h3>{{localize "sf2e-forge-custom.sync.sectionUpdate"}}</h3>
                <ul>
                    {{#each update}}
                        <li>{{name}} <span class="forge-sync-meta">({{docType}})</span></li>
                    {{/each}}
                </ul>
            </section>
        {{/if}}

        {{#if reimport.length}}
            <section class="forge-sync-section">
                <h3>{{localize "sf2e-forge-custom.sync.sectionReimport"}}</h3>
                <ul>
                    {{#each reimport}}
                        <li>
                            <label>
                                <input type="checkbox" name="reimport" value="{{key}}">
                                {{name}}
                                {{#if modifiedInFoundry}}
                                    <i class="fa-solid fa-triangle-exclamation forge-sync-warning" title="{{localize "sf2e-forge-custom.sync.modifiedInFoundry"}}"></i>
                                    <span class="forge-sync-warning-label">{{localize "sf2e-forge-custom.sync.modifiedInFoundry"}}</span>
                                {{/if}}
                            </label>
                        </li>
                    {{/each}}
                </ul>
            </section>
        {{/if}}

        {{#if stale.length}}
            <section class="forge-sync-section">
                <h3>{{localize "sf2e-forge-custom.sync.sectionStale"}}</h3>
                <ul>
                    {{#each stale}}
                        <li>
                            <label>
                                <input type="checkbox" name="deleteStale" value="{{key}}">
                                {{localize "sf2e-forge-custom.sync.staleDeleteLabel" name=name docType=docType}}
                            </label>
                        </li>
                    {{/each}}
                </ul>
            </section>
        {{/if}}

        {{#unless hasWork}}
            <p class="forge-sync-empty">{{localize "sf2e-forge-custom.sync.nothingToApply"}}</p>
        {{/unless}}

        <footer class="forge-sync-footer">
            <button type="submit" {{#unless hasWork}}disabled{{/unless}}>
                <i class="fa-solid fa-check"></i> {{localize "sf2e-forge-custom.sync.apply"}}
            </button>
        </footer>
    {{/if}}
</div>
